[package]
name = "nuxt-edge-ai-kernel"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Rust WASM kernel for nuxt-edge-ai."

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wasm-bindgen = "0.2"
